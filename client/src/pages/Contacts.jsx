import React,{useEffect,useState} from 'react'
import useStore from '../store'
import { useNavigate } from 'react-router-dom'
export default function Contacts(){ const nav=useNavigate(); const { loadAll, users, openChat, setActiveChatId } = useStore(); const [q,setQ]=useState(''); useEffect(()=>{ loadAll() },[]); const list = users.filter(u => (`${u.firstName} ${u.lastName} ${u.profession} ${u.city} ${u.gothram}`).toLowerCase().includes(q.toLowerCase())); return (<div style={{padding:16}}><h2 style={{color:'#7A1C10'}}>Community Contacts</h2><input className='input' placeholder='Search' value={q} onChange={e=>setQ(e.target.value)} style={{margin:'8px 0 12px'}}/><div style={{display:'grid',gap:10}}>{list.map(u=>(<div key={u.id} className='card' style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><div style={{fontWeight:800}}>{u.firstName} {u.lastName}</div><div style={{fontSize:12,color:'#7a6a55'}}>{u.gothram} • {u.profession} • {u.city}</div></div><button className='btn' onClick={async()=>{ const id=await openChat(`${u.firstName} ${u.lastName}`); setActiveChatId(id); nav('/chats'); }}>Chat</button></div>))}</div></div>) }