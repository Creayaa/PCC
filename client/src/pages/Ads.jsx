import React,{useEffect,useState} from 'react'
import useStore from '../store'
import { useNavigate } from 'react-router-dom'
export default function Ads(){ const nav=useNavigate(); const { loadAll, ads, postAd, openChat, setActiveChatId }=useStore(); useEffect(()=>{ loadAll() },[]); const [form,setForm]=useState({title:'',category:'Job',location:'',address:'',description:''}); return (<div style={{padding:16}}><h2 style={{color:'#7A1C10'}}>Ads</h2><div className='card'><div style={{display:'grid',gap:8}}><input className='input' placeholder='Title' value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><select className='input' value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option>Job</option><option>Service</option><option>Land</option><option>Travel</option></select><input className='input' placeholder='Location' value={form.location} onChange={e=>setForm({...form,location:e.target.value})}/><input className='input' placeholder='Address' value={form.address} onChange={e=>setForm({...form,address:e.target.value})}/><textarea className='input' placeholder='Description' value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><button className='btn' onClick={()=>{ postAd(form); setForm({title:'',category:'Job',location:'',address:'',description:''}) }}>Post Now</button></div></div><div style={{display:'grid',gap:10, marginTop:12}}>{ads.map(a=>(<div key={a.id} className='card' style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><div style={{fontWeight:800,color:'#7A1C10'}}>{a.title}</div><div style={{fontSize:12,color:'#7a6a55'}}>{a.category} • {a.location}</div><p style={{marginBottom:0}}>{a.description}</p></div><button className='btn' onClick={async()=>{ const id=await openChat('Ad: '+a.title); window.__store = (await import('../store')).default; window.__store.getState().sendMessage(id,'I am interested on this job lets discuss.'); setActiveChatId(id); nav('/chats'); }}>Accept</button></div>))}</div></div>) }